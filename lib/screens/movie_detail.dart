import 'package:flutter/material.dart';

class MovieDetaiPage extends StatelessWidget {
  const MovieDetaiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('MovieDetail Page'),
    );
  }
}